@model List<Animal>

    <div>
    <ul>
    @{
        foreach(Animal a in Model)
        {
            <h1>@a.Name</h1>
            <img height="500px" src="@a.Img" />
            <p>Description: @a.Description</p>

        }

        }

    </ul>
</div>